import { test } from '@playwright/test';

// Placeholder: Não há integração Stripe no repositório atual.
// Este teste fica como pendente até existir backend e webhook Stripe.
// Critérios: criar cliente, iniciar Checkout Session e validar webhook.
test.skip('Stripe MCP — fluxo de pagamento (pendente)', async () => {
  // A implementar quando Stripe/back-end estiver disponível.
});
