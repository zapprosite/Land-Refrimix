VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=http://localhost:4000

# Modelo de IA utilizado pelo front para exibição (sem chave no cliente)
VITE_OPENAI_MODEL=gpt-4o-mini

# Prompt opcional do assistente (cuidado com tamanho)
VITE_AI_SYSTEM_PROMPT=Você é um assistente de vendas HVAC-R focado em orientação de compra, sem instruções de instalação.

# (Recomendado) Proxy/backend para chamadas de IA. O front nunca deve enviar a chave diretamente para provedores.
VITE_AI_PROXY_URL=http://localhost:4000/api/ai/chat

